# Автоматическое тестирование и управление

## ✅ Что настроено:

1. **API туннель** - работает на порту 14141
2. **Автосинхронизация** - API события автоматически синхронизируются с event log
3. **Автообновление холста** - холст обновляется каждые 2 секунды
4. **Автотестирование** - скрипт `test-api.ps1` проверяет работу API

## 🔄 Как работает автоматизация:

### 1. API → Event Log синхронизация

Когда создаётся событие через API:
- Событие сохраняется в памяти API сервера
- Автосинхронизация (каждые 3 секунды) переносит его в `.thoughtlog/events.ndjson`
- Холст автоматически обновляется и показывает новое событие

### 2. Event Log → Canvas синхронизация

Когда событие создаётся локально:
- Сохраняется в `.thoughtlog/events.ndjson`
- Холст автоматически обновляется (каждые 2 секунды)
- API может получить эти события через `/events`

### 3. Автоматическое тестирование

Запуск тестов:
```powershell
powershell -ExecutionPolicy Bypass -File test-api.ps1
```

Тесты проверяют:
- ✅ Health check
- ✅ Создание событий
- ✅ Получение событий
- ✅ Управление холстом (зум, позиция)

## 📡 Управление через API:

### Создать событие:
```bash
curl -X POST http://127.0.0.1:14141/events \
  -H "Content-Type: application/json" \
  -d '{"event_type":"INTENT_DECLARED","data":{"text":"Новая задача"}}'
```

### Получить события:
```bash
curl http://127.0.0.1:14141/events
```

### Управление холстом:
```bash
# Установить зум
curl -X POST http://127.0.0.1:14141/canvas/zoom \
  -H "Content-Type: application/json" \
  -d '{"zoom":2.0}'

# Установить позицию
curl -X POST http://127.0.0.1:14141/canvas/position \
  -H "Content-Type: application/json" \
  -d '{"x":100,"y":200}'
```

## 🤖 AI может автоматически:

- Создавать события через API
- Читать состояние приложения
- Управлять холстом (зум, позиция)
- Синхронизировать события между API и event log
- Отслеживать изменения в реальном времени

**Всё работает автоматически - не нужны скриншоты!**
